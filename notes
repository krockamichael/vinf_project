Create topic
    .\bin\windows\kafka-topics.bat --create --zookeeper localhost:2181 --replication-factor 1 --partitions 1 --topic final_topic

Delete topic
    DON'T DO THIS, KAFKA_SERVER WON'T START AFTER
    KAFKA_SERVER WON'T START AFTER
    KAFKA_SERVER WON'T START AFTER
    KAFKA_SERVER WON'T START AFTER
    KAFKA_SERVER WON'T START AFTER

    -- delete everything from zookeeper/data,
                              zookeeper/logs,
                              kafka/logs,
                              kafka/kafka-logs

    .\bin\windows\kafka-topics.bat --zookeeper localhost:2181 --delete --topic final_topic

# TODO better solution for encapsulating tag